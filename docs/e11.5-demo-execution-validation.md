# E11.5 Demo execution validation plan

This milestone supplies an execution foundation only. It does not place an order, migrate a database, or wire strategy/Paper signals to a broker.

When a separately authorized Demo test is scheduled, verify disabled-by-default behavior first, then readiness refusal for Real/Contest mode, missing server/fingerprint, disabled account trading, and disabled EA trading. Verify `OrderCheck` rejection leaves an auditable `Rejected` intent. For an accepted controlled order, record the exact ticket/deal, reconcile it after a simulated timeout, and close it by its exact ticket. Confirm duplicate client execution IDs never create another broker request.

E11.5A compiled `EmaBotBridge.mq5` on 2026-08-17 with MetaEditor at `C:\Program Files\MetaTrader 5\metaeditor64.exe`: 0 errors and 0 warnings. Generated `.ex5` and compiler-log artifacts are not tracked.

Runtime database migration and controlled Demo lifecycle validation remain pending because this workspace does not establish that the configured user-secret connection is an intended local/disposable Demo database. No Demo order was submitted. When explicitly configured, validate the complete lifecycle (readiness, preflight, one small Demo order, open reconciliation, reconnect reconciliation, exact-ticket close, and closed-history reconciliation) and a failure-injected missing-ticket recovery without creating a duplicate order.
