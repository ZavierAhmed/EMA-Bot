import { useEffect, useState } from 'react'
import { addMonitoredSymbol, getInstruments, getMarketProviderCapabilities, getMonitoredSymbols, removeMonitoredSymbol, setSymbolEnabled, type InstrumentCatalogItem, type MarketProviderCapabilities, type MonitoredSymbol } from '../api'

export function SymbolsPage() {
  const [monitored, setMonitored] = useState<MonitoredSymbol[]>([])
  const [catalog, setCatalog] = useState<InstrumentCatalogItem[]>([])
  const [error, setError] = useState<string | null>(null)
  const [isLoading, setIsLoading] = useState(true)
  const [capabilities, setCapabilities] = useState<MarketProviderCapabilities | null>(null)

  async function refresh() {
    setError(null); setIsLoading(true)
    try {
      const [symbols, providerCapabilities] = await Promise.all([getMonitoredSymbols(), getMarketProviderCapabilities()])
      setMonitored(symbols); setCapabilities(providerCapabilities)
      if (providerCapabilities.instrumentCatalogConfigured) setCatalog(await getInstruments())
      else setCatalog([])
    } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Could not load MT5 instruments.') }
    finally { setIsLoading(false) }
  }

  useEffect(() => { void refresh() }, [])
  async function toggle(symbol: MonitoredSymbol) { try { await setSymbolEnabled(symbol.id, !symbol.isEnabled); await refresh() } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Could not update instrument.') } }
  async function add(symbol: string) { try { await addMonitoredSymbol(symbol); await refresh() } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Could not add the MT5 instrument.') } }
  async function remove(id: number) { try { await removeMonitoredSymbol(id); await refresh() } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Could not remove instrument.') } }
  const isMonitored = (symbol: string) => monitored.some(item => item.source === 'Mt5Exness' && item.symbol === symbol)

  return <div className="space-y-8"><div><p className="text-sm font-medium text-slate-500">Market data</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Symbols</h1><p className="mt-3 text-slate-600">New research uses exact MT5/Exness Market Watch symbols. Legacy Binance symbols are retained only for historical result reproducibility.</p></div><p className="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900">Paper Trading remains blocked pending broker-aware Exness sizing and trading costs.</p>{error && <p role="alert" className="rounded-md bg-red-50 px-3 py-2 text-sm text-red-700">{error}</p>}<section className="rounded-lg border border-slate-200 bg-white"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-200 p-5"><div><h2 className="font-semibold">MT5 Market Watch catalog</h2><p className="mt-1 text-sm text-slate-500">Catalog status: {capabilities?.instrumentCatalogConfigured ? 'Configured' : capabilities ? 'Not configured' : 'Checking…'}.</p></div><button onClick={() => void refresh()} className="rounded-md border border-slate-300 px-3 py-2 text-sm font-medium hover:bg-slate-50">Refresh</button></div><div className="p-5">{isLoading ? <p className="text-sm text-slate-500">Loading instruments...</p> : catalog.length === 0 ? <p className="text-sm text-slate-500">Connect the MT5 bridge to view selected Market Watch instruments.</p> : <table className="w-full text-left text-sm"><thead className="text-slate-500"><tr><th className="pb-3 font-medium">Broker symbol</th><th className="pb-3 font-medium">Description</th><th className="pb-3 font-medium">Base</th><th className="pb-3 font-medium">Profit</th><th /></tr></thead><tbody>{catalog.filter(item => item.isSelected).map(item => <tr key={item.spec.brokerSymbol} className="border-t border-slate-100"><td className="py-3 font-medium">{item.spec.brokerSymbol}</td><td>{item.description ?? item.spec.displaySymbol ?? '—'}</td><td>{item.spec.currencyBase ?? '—'}</td><td>{item.spec.currencyProfit ?? '—'}</td><td className="text-right">{isMonitored(item.spec.brokerSymbol) ? <span className="text-slate-500">Already monitored</span> : <button onClick={() => void add(item.spec.brokerSymbol)} className="text-slate-700 hover:text-slate-950">Add</button>}</td></tr>)}</tbody></table>}</div></section><section className="rounded-lg border border-slate-200 bg-white"><div className="border-b border-slate-200 p-5"><h2 className="font-semibold">Monitored instruments</h2></div><div className="p-5">{isLoading ? <p className="text-sm text-slate-500">Loading instruments...</p> : monitored.length === 0 ? <p className="text-sm text-slate-500">No instruments are monitored yet.</p> : <table className="w-full text-left text-sm"><thead className="text-slate-500"><tr><th className="pb-3 font-medium">Source</th><th className="pb-3 font-medium">Symbol</th><th className="pb-3 font-medium">Description</th><th className="pb-3 font-medium">Base</th><th className="pb-3 font-medium">Quote</th><th className="pb-3 font-medium">Status</th><th /></tr></thead><tbody>{monitored.map(symbol => <tr key={symbol.id} className="border-t border-slate-100"><td className="py-3">{symbol.sourceLabel}</td><td className="font-medium">{symbol.symbol}</td><td>{symbol.displayName ?? '—'}</td><td>{symbol.baseAsset ?? '—'}</td><td>{symbol.quoteAsset ?? '—'}</td><td>{symbol.isEnabled ? 'Enabled' : 'Disabled'}</td><td className="space-x-3 text-right">{symbol.source === 'LegacyBinance' && !symbol.isEnabled ? <span className="text-slate-500">Legacy only</span> : <button onClick={() => void toggle(symbol)} className="text-slate-600 hover:text-slate-950">{symbol.isEnabled ? 'Disable' : 'Enable'}</button>}<button onClick={() => void remove(symbol.id)} className="text-red-700 hover:text-red-900">Remove</button></td></tr>)}</tbody></table>}</div></section></div>
}
